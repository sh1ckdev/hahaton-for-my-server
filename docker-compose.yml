services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-http://localhost:5000/api}
    container_name: rational-assistant-frontend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${EXTERNAL_PORT:-3001}:3001"
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:5000/api}
    networks:
      - rational-assistant-network

networks:
  rational-assistant-network:
    driver: bridge

